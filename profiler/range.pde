// Stores the range of a sample set of values.
public class Range {
  public double min_val;
  public double max_val;
  
  public Range(double min, double max) {
    min_val = min;
    max_val = max;
  }
}
